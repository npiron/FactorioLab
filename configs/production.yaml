run:
  name: "production"  # This triggers production agent
  max_steps: 25  # Enough for full production line
  seed: 42

paths:
  runs_dir: "runs"

agent:
  prompt_path: "src/factorio_ai_lab/prompts/early_bootstrap.txt"
